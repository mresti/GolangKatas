language: go
sudo: false
go:
  - 1.7.x
  - 1.8.x
  - 1.9.x
  - master
matrix:
  allow_failures:
    - go: master
  fast_finish: true

before_script:
  - go fmt ./...
  - go vet $(go list ./...)

script:
  - go test -v -race ./...

after_script:
  - go build $(go list ./... | grep -v /vendor/)